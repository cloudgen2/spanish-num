#[derive(Copy, Clone)]
pub enum Sex {
    Any,
    Male,
    Female
}


#[derive(Copy, Clone)]
pub enum Fruit {
    Any,
    Apple,
    Orange,
    Banana,
    Grape,
    Strawberry,
    Pear,
    Cherry,
    WaterMelon
}

#[derive(Copy, Clone)]
pub enum Animal {
    Any,
    Bird,
    Cat,
    Dog,
    Fish,
    Horse,
    Pig,
    Rabbit
}

#[derive(Copy, Clone)]
pub enum Drink {
    Any,
    Beer,
    Coffee,
    Milk,
    Tea,
    Water,
    Wine
}

#[derive(Copy, Clone)]
pub enum Food {
    Any,
    Bread,
    Croissant,
    Cake,
    Pizza,
    Rice,
    Soup
}

#[derive(Copy, Clone)]
pub enum Transport {
    Ambulance,
    Any,
    Bus,
    Car,
    Taxi
}